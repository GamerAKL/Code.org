onEvent("id", "change", function( ) {
  // id is the name of the dropdown
  //id change menas whenerver they change
  // first option has to be a zero width space
  setText("di", "no bugs pls");
  //sets the text
});
 
  // beter example
  
 onEvent("id", "change", function( ) {
if (getText("id") == "test") 
 setImageURL("image1", "https://urlname");
 });
